
/**
 * Response to a request to update a resource's health
 */
export type RegistrationapiHealthResponse = {
    
    /**
     * Timestamp indicating the time in seconds at which the server recorded the heartbeat
     */
    health : string
};